meta {
  name: Delete Admin Users {id}
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/admin/users/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "force": false
  }
}

settings {
  encodeUrl: true
}
