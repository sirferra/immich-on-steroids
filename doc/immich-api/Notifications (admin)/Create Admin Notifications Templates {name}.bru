meta {
  name: Create Admin Notifications Templates {name}
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/notifications/templates/:name
  body: json
  auth: inherit
}

params:path {
  name: 
}

body:json {
  {
    "template": ""
  }
}

settings {
  encodeUrl: true
}
