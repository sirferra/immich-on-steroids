meta {
  name: Create Admin Maintenance Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/maintenance/login
  body: json
  auth: inherit
}

body:json {
  {
    "token": ""
  }
}

settings {
  encodeUrl: true
}
