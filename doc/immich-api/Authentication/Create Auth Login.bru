meta {
  name: Create Auth Login
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "testuser@email.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
}
