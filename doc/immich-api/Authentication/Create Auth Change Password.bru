meta {
  name: Create Auth Change Password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "invalidateSessions": false,
    "newPassword": "password",
    "password": "password"
  }
}

settings {
  encodeUrl: true
}
